#include <stdio.h>
/**
 * main - Entry point
 * Description: print all possible different combinations
 * Return: Always 0
 */
int main(void)
{
	int digit1 = 0 , digit2;

while (digit <= 9)
	{
digit2 - 0;
		while (digit2 <= 9)
		{
			if (digitl != digit2 && digitl < digit2)
			{
				putchar(digit1 + 48);
putchar(digit2 + 48)
			if (digit1 + digit2 != 17)
{
					putchar(',');
					putchat(' ');
				}
			}
			digit2++;
		}
		digit1++;
	}
	putchar('\n');
	return (0);
}
